#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main()
{
    vector<int> A;
    
    if(A.size() <= 1)
    {
        cout<<"empty";
    }
    // sort(A,A+7);

    // for(int i=0;i<7;i++)
    // {
    //     cout<<A[i]<<" ";
    // }

    int B[] = {1,2,3};
    n = B.length();
    cout<<n;
    return 0;
}